<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Mongoose Dashboard Assignment - Index</title>
</head>
<body>
    <h1>Index Page!</h1>
    <a href="/new">Create new dog</a>
    <% for (const dog in dogs) { %>
        <p>Name: <a href="/<%= dog._id %>"><%= dog.name %></a>
            <a href="/<%= dog._id %>/edit/"><button>Edit dog</button></a>
            <form action="/<%= dog._id %>/delete/" method="POST">
                <input type="submit" value="Delete">
            </form>
        </p>
    <% } %>
</body>
</html>